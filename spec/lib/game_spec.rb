require 'spec_helper'

describe Game do
  describe "upon creation" do
    it "creates the requested number of players"
    it "grabs the default card_deck"
    it "deals the correct number of cards to each player"
    it "waits for the first player action"
  end
end